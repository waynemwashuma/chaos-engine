import {Behaviour} from "./behaviour.js"
import { Vector } from "../../math/index.js"


/**
 * not complete.
*/
class Flock{
  constructor(){
    this.neighbours = []
  }
    /**
     * @inheritdoc
     * @param {Agent} agent
     */
  init(){
    
  }
    /**
     * @inheritdoc
     * @param {Vector} target
     */
  calc(target){
    
  }
}

export{
  Flock
}